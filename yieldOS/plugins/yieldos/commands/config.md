---
allowed-tools: Bash(yieldos-config:*)
description: Initialize, show, or validate yieldOS runtime config
---

# Config

Inspect or create the repo runtime config:

```bash
yieldos-config $ARGUMENTS
```

Useful variants:

```bash
yieldos-config show
yieldos-config init
yieldos-config init --write
yieldos-config validate
```
